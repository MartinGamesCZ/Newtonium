{
  "name": "default",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "start:renderer": "cd src/view && next dev",
    "start": "NODE_ENV=development NEWTONIUM_DEV=true bun --watch src/index.ts",
    "build:renderer": "cd src/view && next build",
    "run": "bun run dist/index.ts"
  },
  "dependencies": {
    "newtonium": "file:../../lib"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
